# Codex forge-loop target exhaustion matrix

Updated: 2026-06-27

This matrix is the current "mined to the bone" checklist for the explicit target set. It separates source extraction from the local `.codex` application and the verifier that prevents regressions.

| Target | Exhausted extraction categories | Applied local surfaces | Regression guard |
| --- | --- | --- | --- |
| `https://github.com/openai/codex` | Codex Rust CLI install/run modes, ChatGPT-plan authentication, `codex exec` noninteractive execution, JSON output, explicit permission-profile/approval flags, and upstream release surface. | `codex_invocation()` forces `exec`, `--json`, `--ignore-user-config`, explicit `default_permissions="forge-loop-workspace"`, `approval_policy="on-request"`, and explicit continuity config for nested scheduled runs. | `codex_invocation_uses_noninteractive_json_permission_profile`; `target_mining_audit_covers_full_research_source_matrix`; `target-mining-audit`. |
| `https://developers.openai.com/codex/config-advanced` | Project config, hooks, rules, custom agents, model flags, sandbox settings, and auto-compaction controls. | `.codex/config.toml`; `.codex/hooks.json`; `.codex/rules/forge-loop.rules`; `.codex/agents/*`; `.codex/prompts/compact-forge-loop.md`; explicit `codex_invocation()` config flags. | `forge_loop_config_enables_auto_compaction`; `codex_invocation_forces_auto_compaction_continuity`; `target_mining_audit_covers_full_research_source_matrix`. |
| `developers.openai.com/codex/github-action` | Trigger authorization, prompt source, model/effort controls, artifact output, structured schema, troubleshooting evidence. | `.github/workflows/codex-forge-loop.yml`; `.github/codex/prompts/forge-loop.md`; `.github/codex/schemas/forge-loop-output.schema.json`; `output-schema-audit`. | `codex_github_action_workflow_uses_documented_controls`; `output_schema_audit_requires_subscription_auth_inventory_and_continuity`; `target-mining-audit`. |
| `developers.openai.com/codex/permissions` | Built-in profiles, custom `default_permissions`, filesystem precedence, secret deny globs, writable git metadata, sibling worktree roots, runtime proof paths, stale-shadow cleanup paths, bounded glob scan, network domain allowlists, local/private-network caution, no mixing with `sandbox_mode`. | active `.codex/config.toml`; `.codex/permissions/forge-loop-workspace.toml`; `.codex/hooks/forge_loop_permission_request.py`; `codex_invocation()` passes the active profile and on-request approval policy for merge-cleanup sessions. | `codex_deep_target_mining_surfaces_are_guarded`; `codex_permissions_profile_enables_merge_cleanup_session`; `codex_invocation_uses_active_permission_profile_with_escalation_path`; `target-mining-audit`. |
| `developers.openai.com/codex/subagents` | Built-ins, project `.codex/agents/*.toml`, required fields, optional model/sandbox/skills/MCP config, `nickname_candidates` display labels, fan-out caps, inherited sandbox behavior, explicit spawn requirement. | `.codex/agents/forge-loop-auditor.toml`; `.codex/agents/forge-loop-researcher.toml`; `.codex/agents/forge-loop-ci-sentinel.toml`; SubagentStart/SubagentStop hooks. | `components-audit --strict`; `target-mining-audit`. |
| `RoggeOhta/awesome-codex-cli` | Ecosystem categories: AGENTS templates, subagents, skills, plugins, hooks, MCP, workflow/session managers, CI/CD, Monitoring/analytics, Docker/sandboxing, cross-agent tools. | Forge-loop research skill requires inventory across those categories; target ledger records extracted pressure; components-audit includes tools, skills, agents, hooks, docs. | `forge_loop_skill_references_codex_config_and_action_docs`; `target-mining-audit`. |
| `Yeachan-Heo/oh-my-codex` | Strong-session launch, named worktree isolation, doctor/smoke tests, deep-interview -> plan -> durable-goal path, native hook mapping, durable state/logs, teams only for parallelizable work, stale-team cleanup. | Forge-loop prompt requires source-attributed mining, isolated named worktrees, explicit subagent fan-out only when useful, and final component/docs-drift gates; compact hooks preserve target coverage across context churn. | prompt mirror tests plus `target-mining-audit`. |
| `https://crates.io` | Rust crates for scheduling, tracing, structured output, evaluation, and reliability; strict-upgrade dependency governance before additions. | Existing workspace dependencies provide structured JSON artifacts and contextual errors without adding dependency churn in this cycle. | `target_mining_audit_covers_full_research_source_matrix`; `doctor_json_exports_required_gate_contract`; `target-mining-audit`. |
| `drdave-flexnetos/kclaw0` | 24/7 autonomous operation, heartbeat scheduler, 300-agent / 4000-step / 12+ hour persistence evidence, 120-agent swarm target, self-upgrade pipeline, immutable governance, holdout validation, per-node budget caps, and GitHub label state machine. | `docs/forge-loop/kclaw0-runner-flow-target.md`; `docs/forge-loop/agentic-system-proof.md`; `Runner Sustain`; `Runner Black Factor Watch`; `Agentic System Watch`; `Codex Forge Loop`; `runner-black-factor-audit --strict`; `runner-ops-slo-audit --strict`; `runner-fleet-audit --strict`; `runner-queue-audit --strict`; `agentic-system-audit --strict`. | `runner_black_factor_audit_accepts_kclaw0_window_fixture`; `runner_ops_slo_audit_accepts_event_rehydrated_burn_in`; `runner_ops_slo_audit_counts_codex_growth_as_productive_runner_work`; `runner_fleet_audit_default_scope_accepts_all_flexnetos_org_repos`; `runner_queue_audit_classifies_local_waits_and_nonlocal_queues`; `agentic_system_audit_report`; `agentic_system_watch_dispatches_codex_growth_safely`; `target-mining-audit`. |
| kclaw0 referenced resources | strongdm/attractor checkpoint/resume and human-gate workflows; coleam00/Archon workflow gates, cleanup, classifier, and Dark Factory state-machine patterns; Conway-Research/automaton heartbeat, survival tiers, policy audit, and authority/allowlist patterns; oh-my-pi task tools, registries, session management, async jobs, markdown agents, blob storage, and auto-compaction. | `docs/kclaw0-upgrade-ledger.md` applied rows; Cycle-11 and Cycle-16 deep-research sweeps; prior-art batch widened beyond kclaw0 to the sources it names. | `target-mining-audit`; `target_mining_audit_proves_sources_applications_and_guards`. |

## Remaining policy

A future loop may add more surfaces, but it must not claim a target has been mined unless the extraction category, local application, and regression guard are all updated together. If official Codex docs change, update this matrix and `fxrun forge-loop target-mining-audit --strict` in the same PR.

## 2026-06-27 auto-compaction continuity extraction

All seven target families now feed the `.codex` continuity contract: local config enables `auto_compaction`, `model_auto_compact_token_limit`, scoped tool-output limits, and `experimental_compact_prompt_file`; `codex exec` invocations and the GitHub Action pass the same settings explicitly; `PreCompact`/`PostCompact` hooks emit target coverage; and the structured output schema requires `auto_compact_continuity` evidence. This applies the OpenAI context-window/compaction guidance, the OMX durable-state pattern, the kclaw0 24/7 dark-factory requirement, and the kclaw0 referenced-resource prior art without adopting an external runtime.
