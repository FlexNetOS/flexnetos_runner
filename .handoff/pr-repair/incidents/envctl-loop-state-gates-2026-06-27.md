# PR Repair Incident — envctl loop-state gates failure

Created: 2026-06-27
Status: prototype incident / ready for shared repair lane
Classification: `cross_pr_shared_failure`
Supervisor task: `TASK-849A22-0001`

## Summary

The first `.idea` → `.handoff` prototype execution promoted the V5 brainstorm into a claimed
handoff task, resynced the local handoff kernel, and inspected the repeated envctl gates failures
called out by the earlier lesson.

The two live PRs still share the same failure signature:

| PR | Branch | CI run | Gates job | Result |
|---|---|---:|---:|---|
| FlexNetOS/envctl#284 | `plan/loop-prompt-hub` | `28281171897` | `83797089887` | gates failed |
| FlexNetOS/envctl#281 | `plan/loop-grit` | `28280061204` | `83793897460` | gates failed |

Both jobs pass the preceding gates and then fail in `bash ci/gates/loop-state.sh` with:

```text
LOOP-STATE GATE FAIL — .handoff/loop/plan/loop_state.md: cycle_budget is not a non-negative integer (got: '<missing>')
```

## Supervisor decision

Treat this as one shared envctl loop-state incident, not two independent PR regressions.
The shared failing path is `.handoff/loop/plan/loop_state.md`; assigning separate workers to PR
#284 and PR #281 would duplicate diagnosis and spend both runner slots on the same root cause.

## Next worker lane

Create a single envctl repair lane for the shared base fix, then decide whether the repair belongs
on the common base branch or must be replayed into both PR heads.

Suggested startup:

```bash
cd /home/drdave/Desktop/meta
rtk meta project list --json
rtk meta git status
rtk meta git worktree list --json
rtk meta git worktree create repair-envctl-loop-state-gates --repo envctl --from-pr FlexNetOS/envctl#284 --ttl 24h --meta incident=envctl-loop-state-gates-2026-06-27 --json
cd /home/drdave/Desktop/meta/.worktrees/repair-envctl-loop-state-gates/envctl
```

First commands inside the lane:

```bash
gh pr view 284 --repo FlexNetOS/envctl --json number,title,url,state,headRefName,baseRefName,statusCheckRollup
gh pr view 281 --repo FlexNetOS/envctl --json number,title,url,state,headRefName,baseRefName,statusCheckRollup
gh run view 28281171897 --repo FlexNetOS/envctl --job 83797089887 --log | tail -120
gh run view 28280061204 --repo FlexNetOS/envctl --job 83793897460 --log | tail -120
```

## Stop conditions

- Green gates on both affected PRs, or both PRs superseded/closed.
- Evidence that only one PR still carries the failure after rerun; then split the remaining PR into
  an individual repair lane.
- Evidence that the failure is caused by an external policy/tooling regression outside envctl; then
  escalate to the supervisor with the exact failing command and log excerpt.

## Handoff notes

- This incident is a `.handoff` execution artifact. The originating brainstorm remains in
  `.handoff/.idea` and is not active queue state.
- Binary ledger caches remain local-only and gitignored; `.handoff/ledger.events.jsonl` is the text
  continuity export when generated by `hf`.
