# Handoff Packet (latest) — handoff.packet.v2

> Compiled by `hf fleet render flexnetos_runner` from the FLEET ledger (meta/.handoff) + this repo's git-text capsule/cards. Not rendered from a per-repo ledger (ADR-0004 §3).

## 1. North Star (flexnetos_runner (ops/execution plane))
A local runner+app to connect all of meta seamlessly (ADR-0008 execution plane).

## 2. State Precedence
Git > FLEET ledger (meta/.handoff/ledger.db) > tasks/*.task.json > this packet.

## 3. Progress
Done: 0/0.  FLEET tamper-evident events verified: 0.

## 4. Remaining
- (no open cards)

